from Spell_class import Spell_class

def build_response(spell_name_in):
    spell = Spell_class(spell_name_in)

    if spell._name is not 'empty':
        response = 'Name ' + spell._name + '\n'
    
    if spell._desc is not 'empty':
        

        self._name
        self._desc
        self._higher_level
        self._range
        self._components
        self._material
        self._ritual
        self._duration
        self._concentration
        self._casting_time
        self._level
        self._attack_type
        self._damage_type
        self._damage_at_slot_level
        self._heal_at_slot_level
        self._damage_at_character_level
        self._heal_at_character_level
        self._dc_type
        self._dc_success
        self._area_of_effect_type
        self._area_of_effect_size
        self._school